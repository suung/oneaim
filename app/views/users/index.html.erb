<h1>Listing users</h1>

<table>
  <tr>
    <th>Email</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%=h user.email %></td>
    <td><%= show_link user %></td>
    <td><%= edit_link user %></td>
    <td><%= delete_link user %></td>
  </tr>
<% end %>
</table>

<br />

<%= new_link :user %>
